@model IEnumerable<BTLnhom07.Models.SanPham>

@{
    ViewData["Title"] = "Index";
}

<h1>SanPham</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SanPhamCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SanPhamName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SanPhamCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SanPhamName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SanPhamCode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SanPhamCode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SanPhamCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
